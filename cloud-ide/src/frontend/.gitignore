# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Generated by `npm run icons:build` (auto-run via pre-dev/pre-build) from the icon
# registry + @iconify-json/* data packages. A build artifact, so it is not committed —
# run `npm run icons:build` after a fresh clone or after editing the icon registry.
src/common/icons.offline.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

src/terminal/transport/__tests__/
